---
# tasks file for testing_petclinic
- name: Starting tests
  shell:
    cmd: mvn spring-javaformat:apply -Dcheckstyle.skip test
    chdir: "{{ home_dir }}"
  register: result
  
- debug:
  var: result > /home/ubuntu/tests-logs.txt
